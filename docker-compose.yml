version: "3.9"

services:
  contentserver:
    image: ghcr.io/victory-family-church/contentserver:latest
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./contentserver.db:/app/contentserver.db
    env_file:
      - .env
    restart: unless-stopped
